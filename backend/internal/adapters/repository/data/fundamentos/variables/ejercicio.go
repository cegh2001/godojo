//go:build ignore

package main

// Saludar retorna un saludo personalizado usando el nombre recibido.
// Ejemplo: Saludar("Gopher") debe devolver "¡Hola, Gopher!"
func Saludar(nombre string) string {
	// TODO: Devolver "¡Hola, " + nombre + "!"
	return ""
}

// Sumar retorna la suma de dos números enteros.
func Sumar(a, b int) int {
	// TODO: Devolver la suma de a + b
	return 0
}

// EsPar retorna true si el número es par (el resto de dividir por 2 da 0).
// Pista: usá el operador % (módulo) que devuelve el resto de una división.
func EsPar(n int) bool {
	// TODO: Verificar si n % 2 == 0
	return false
}
